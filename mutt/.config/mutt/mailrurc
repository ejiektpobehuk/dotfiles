set imap_user = "ejiek@mail.ru"                       
set smtp_url = "smtp://ejiek@mail.ru:465/"       
set realname = "Petrov V.D."                                  

set folder = "imaps://imap.mail.ru:993"                     
set spoolfile = "+INBOX"                                      
set record = /dev/null
set postponed = "+[mailru]/Drafts"                             
set header_cache = ~/.config/mutt/mailru/cache/headers            
set message_cachedir = ~/.config/mutt/mailru/cache/bodies         
set certificate_file = ~/.config/mutt/mailru/certificates  
set ssl_starttls = yes
set ssl_force_tls = yes
macro index gi "<change-folder>=INBOX<enter>" "Go to inbox"
macro index gp "<change-folder>=INBOX.Poly<enter>" "Go to study mail"
macro index gj "<change-folder>=INBOX.PolyJob<enter>" "Go to work mail"

#For passwords, if you want login automatic:
set smtp_pass=$my_pw_mailru
set imap_pass=$my_pw_mailru
